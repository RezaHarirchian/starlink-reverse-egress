# 🌐 اتصال‌پذیری برون‌مرزی در شرایط اختلال شدید ارتباطات

## راهنمای معماری برای دو سناریوی عملی (بدون جزئیات اجرایی)

---

## 📌 هدف این مستند

این README برای توضیح **دو معماری متفاوت** در شرایطی است که ارتباطات بین‌المللی یک شبکه/کشور به‌شدت مختل یا قطع می‌شود.  
هدف، کمک به تصمیم‌گیری درست درباره این است که **کدام سناریو در چه وضعیتی قابل استفاده است** و چه پیش‌نیازهایی دارد.

> نکته: از ارائه‌ی دستورالعمل‌های اجرایی و قابل سوءاستفاده برای دور زدن محدودیت‌ها/Shutdown خودداری شده است.  
> اگر استفاده‌ی شما **کاملاً قانونی و مجاز** (مثلاً برای DR سازمانی، شبکه‌ی خصوصی مجاز، یا لینک پشتیبان دارای مجوز) است، می‌توانم راهنمای اجرایی را در سطحی که با قوانین و سیاست‌ها سازگار باشد، به شکل ایمن‌تر بازنویسی کنم (مثلاً با ارجاع به مستندات رسمی و چک‌لیست‌های عمومی).

---

## 🧠 اول تعیین کن: Ingress داریم یا نه؟

در عمل، وضعیت «قطع بین‌الملل» می‌تواند این دو حالت را داشته باشد:

### ✅ Scenario A — Ingress برقرار، Egress قطع/محدود
- IP/سرویس داخل ایران **از خارج قابل دسترسی است** (حداقل روی برخی پورت‌ها/پروتکل‌ها)
- ولی سرور داخل ایران **نمی‌تواند به خارج Initiate** کند یا خروجی بین‌المللی ندارد
- در این حالت، **اتصال باید از بیرون Initiate شود** و یک مسیر خروجی بیرونی فراهم کند

**این سناریو مناسب است وقتی:**
- از خارج هنوز می‌شود به یک نقطه داخل ایران TCP/UDP برقرار کرد
- ولی مسیر خروجی بین‌المللی از داخل وجود ندارد یا به‌شدت ناپایدار است

### ✅ Scenario B — Ingress و Egress هر دو قطع (Shutdown مطلق)
- IPهای داخل ایران **از خارج Route نمی‌شوند** (نه ICMP، نه TCP، نه UDP)
- و داخل ایران هم خروجی بین‌المللی ندارد
- در این حالت، معماری‌های مبتنی بر «نقطه‌ی دیتاسنتری داخل» عملاً بی‌معنا هستند

**این سناریو مناسب است وقتی:**
- هیچ نوع دسترسی ورودی از خارج به IP ایران وجود ندارد
- تنها گزینه، **استفاده از لینک مستقل (مثل لینک ماهواره‌ای) داخل مرز** است

---

## 🗺 نمای کلی معماری‌ها

### Scenario A (Ingress Available)

```
User → (Access Protocol) → Iran VPS → (Outside‑Initiated Link) → External Egress → Internet
```

مشخصه کلیدی:
- «Initiator» بیرون از ایران است
- نقطه‌ی ایران نقش «Receiver/Listener» دارد
- خروجی نهایی بیرون از ایران انجام می‌شود

### Scenario B (No Ingress)

```
User → (Access Protocol) → Physical Node (Iran) → Local Independent Link → Internet
```

مشخصه کلیدی:
- لینک مستقل **داخل ایران** برقرار است
- خروجی نهایی از همان داخل، روی لینک مستقل انجام می‌شود
- به مسیر بین‌المللی ISPهای داخلی وابسته نیست

---

## 🧩 پیش‌نیازها (در سطح معماری)

### مشترک
- مدیریت هویت کاربران (User Management)
- کنترل ظرفیت (Rate/Session Limits)
- مانیتورینگ پایدار (Latency / Loss / Uptime)
- برنامه‌ی بازیابی (Failover Plan)

### Scenario A
- یک نقطه‌ی داخل ایران که **Ingress به آن برقرار باشد**
- یک نقطه‌ی بیرون از ایران برای **Egress آزاد**
- روشی برای **Outside‑Initiated Connectivity** (اتصال Initiate‑شده از بیرون)

### Scenario B
- یک نقطه‌ی فیزیکی داخل ایران
- لینک مستقل داخل مرز (مثلاً ماهواره‌ای)
- برق پایدار/UPS و تمهیدات نگهداری فیزیکی
- سیاست‌های ظرفیت و سهمیه‌بندی جدی‌تر (به علت محدودیت لینک)

---

## ⚖️ مقیاس‌پذیری و مدیریت ظرفیت

### Scenario A
- معمولاً مقیاس‌پذیری ساده‌تر است (افزودن نود خروجی، تقسیم بار، چند مسیر خروجی)
- اما به وضعیت Ingress حساس است

### Scenario B
- مقیاس‌پذیری به‌شدت به ظرفیت همان لینک مستقل محدود می‌شود
- معمولاً نیازمند:
  - سهمیه‌بندی
  - محدودیت سشن
  - مدیریت ترافیک و QoS
  - چند نقطه‌ی فیزیکی (در صورت نیاز)

---

## ⚡ Failover و Monitoring (چک‌لیست)

### Monitoring حداقلی
- وضعیت لینک (Uptime)
- Latency و Packet Loss
- تعداد سشن‌ها / کاربران فعال
- مصرف پهنای‌باند

### Failover حداقلی
- سناریوی جایگزین لینک
- ریستارت کنترل‌شده سرویس‌ها
- مکانیزم اعلان (Alerting) به اپراتور

---

## 🧠 جمع‌بندی

- اگر **Ingress برقرار است** → Scenario A مسیر درست است  
- اگر **Ingress هم قطع است** → Scenario B تنها مسیر عملی است  
- این دو سناریو رقیب نیستند؛ انتخاب درست تابع وضعیت Routing است

---

## 🔗 منابع پیشنهادی (مستندات رسمی)
- WireGuard (Official Documentation)
- OpenVPN (Official Documentation)
- V2Ray / Xray (Official Documentation)
- Linux Routing / Netfilter / nftables (Official Docs)

