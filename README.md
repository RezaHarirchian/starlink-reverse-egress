# 🌐 اتصال VPS ایران به اینترنت آزاد در Shutdown کامل

## معماری پایدار، قابل مقیاس و عملی در شرایط قطع بین‌الملل

---

## ⚠️ Scope & Applicability

این مستند **دو سناریوی کاملاً متفاوت و عملی** را پوشش می‌دهد که انتخاب بین آن‌ها وابسته به وضعیت واقعی Routing بین‌المللی IPهای ایران است.

قبل از اجرای هر بخش، باید مشخص شود که آیا **دسترسی ورودی (Ingress)** به IPهای ایران از خارج وجود دارد یا خیر.

سناریوها:

- **Scenario A** – Ingress برقرار است، اما Egress به خارج قطع یا به‌شدت محدود شده  
- **Scenario B** – هم Ingress و هم Egress به‌طور کامل قطع شده‌اند (Shutdown مطلق)

انتخاب سناریوی اشتباه، منجر به پیاده‌سازی غیرقابل‌استفاده خواهد شد.

---

## 📚 Table of Contents

- [📌 مقدمه](#-مقدمه)
- [🎯 هدف معماری](#-هدف-معماری)
- [🧠 اصل فنی مسئله](#-اصل-فنی-مسئله-core-networking-principle)
- [🗺 معماری کلی اتصال](#-معماری-کلی-اتصال)
- [🧩 پیش‌نیازها](#-پیشنیازها)
- [Scenario A – VPS ایران + Starlink خارج](#scenario-a--vps-ایران--starlink-خارج-ingress-available)
- [Scenario B – سرور فیزیکی داخل ایران + Starlink داخل ایران](#scenario-b--سرور-فیزیکی-داخل-ایران--starlink-داخل-ایران-no-ingress)
- [🧠 جمع‌بندی نهایی](#-جمع‌بندی-نهایی)

---

## 📌 مقدمه

این مستند برای سناریویی نوشته شده که اینترنت ایران وارد **Shutdown کامل در لایه Routing بین‌الملل** شده است؛  
نه اختلال، نه محدودسازی، نه فیلترینگ.

به‌صورت دقیق یعنی:

* سرورهای داخل ایران صرفاً دارای Route داخلی هستند  
* هیچ Default Route یا BGP Path به خارج از ASهای داخلی وجود ندارد  
* سرویس‌هایی نظیر DNS Tunneling، CDN، Cloudflare، Warp و روش‌های مشابه عملاً غیرقابل استفاده هستند  

در این شرایط، فقط **معماری‌هایی که مبتنی بر واقعیت Routing طراحی شده‌اند** قابل اجرا هستند.

---

## 🎯 هدف معماری

* تأمین اینترنت آزاد برای کاربران داخل ایران  
* حفظ IP ایران در نقطه اتصال کاربر  
* امکان سرویس‌دهی به تعداد بالای کاربر  
* پشتیبانی از Load Balance، Failover و Monitoring واقعی  

---

## 🧠 اصل فنی مسئله (Core Networking Principle)

در شرایط Shutdown:

- سرور ایران قادر به Initiate اتصال به خارج نیست  
- تنها اتصال‌های **Initiate‌شده از خارج** امکان عبور دارند  

بنابراین، اگر Ingress برقرار باشد، می‌توان از **تانل معکوس (Reverse Tunnel)** استفاده کرد.  
اگر Ingress هم قطع باشد، تنها راه خروج از زیرساخت ISP ایران، استفاده از **Starlink فیزیکی داخل ایران** است.

---

## 🗺 معماری کلی اتصال

```
User
  |
  |  (OpenVPN / WireGuard / V2Ray)
  |
Iranian Exit Node
  |
Free Internet
```

---

## 🧩 پیش‌نیازها

- تشخیص دقیق وضعیت Ingress / Egress  
- انتخاب سناریوی صحیح بر اساس واقعیت شبکه  
- دسترسی کامل مدیریتی به سرور یا دستگاه فیزیکی  

---

# Scenario A – VPS ایران + Starlink خارج (Ingress Available)

> ⚠️ این سناریو فقط زمانی قابل اجراست که IP ایران از خارج **قابل دسترسی باشد**.

---

## 🧠 توضیح سناریو A

- VPS ایران فقط اینترنت داخلی دارد  
- هیچ اتصال خروجی بین‌المللی از ایران ممکن نیست  
- یک نود خارج از کشور با اینترنت آزاد (Starlink) اتصال را Initiate می‌کند  

---

## 🗺 دیاگرام سناریو A

```
User
  |
  |  (OpenVPN / WireGuard / V2Ray)
  |
VPS (Iran)
  |
  |  Reverse Tunnel (WireGuard)
  |
Starlink Node (Outside Iran)
  |
Free Internet
```

---

## 🖥 پیکربندی WireGuard روی VPS ایران (Listen Only)

```bash
apt update
apt install wireguard iptables -y
```

```bash
wg genkey | tee /etc/wireguard/ir_private.key | wg pubkey > /etc/wireguard/ir_public.key
```

`/etc/wireguard/wg0.conf`

```ini
[Interface]
Address = 10.50.0.1/24
ListenPort = 443
PrivateKey = IR_PRIVATE_KEY

PostUp   = sysctl -w net.ipv4.ip_forward=1
PostUp   = iptables -t nat -A POSTROUTING -o wg0 -j MASQUERADE
PostDown = iptables -t nat -D POSTROUTING -o wg0 -j MASQUERADE
```

```bash
wg-quick up wg0
systemctl enable wg-quick@wg0
```

---

## 🌍 پیکربندی WireGuard روی نود Starlink (Initiator)

```bash
apt install wireguard -y
wg genkey | tee sl_private.key | wg pubkey > sl_public.key
```

`/etc/wireguard/wg0.conf`

```ini
[Interface]
Address = 10.50.0.2/24
PrivateKey = STARLINK_PRIVATE_KEY
DNS = 1.1.1.1

[Peer]
PublicKey = IR_PUBLIC_KEY
Endpoint = IR_VPS_IP:443
AllowedIPs = 0.0.0.0/0
PersistentKeepalive = 10
```

```bash
wg-quick up wg0
sysctl -w net.ipv4.ip_forward=1
iptables -t nat -A POSTROUTING -o eth0 -j MASQUERADE
```

---

## 👥 اتصال کاربران به VPS ایران

### OpenVPN

```bash
apt install openvpn easy-rsa -y
```

```ini
port 1194
proto udp
dev tun
server 10.60.0.0 255.255.255.0
push "redirect-gateway def1"
push "dhcp-option DNS 1.1.1.1"
```

```bash
iptables -t nat -A POSTROUTING -s 10.60.0.0/24 -o wg0 -j MASQUERADE
```

---

### WireGuard Client

```ini
[Interface]
PrivateKey = CLIENT_PRIVATE_KEY
Address = 10.70.0.2/32

[Peer]
PublicKey = IR_PUBLIC_KEY
Endpoint = IR_VPS_IP:51820
AllowedIPs = 0.0.0.0/0
PersistentKeepalive = 15
```

---

### V2Ray (VLESS)

```bash
bash <(curl -Ls https://raw.githubusercontent.com/vaxilu/x-ui/master/install.sh)
```

NAT:

```bash
iptables -t nat -A POSTROUTING -o wg0 -j MASQUERADE
```

---

# Scenario B – سرور فیزیکی داخل ایران + Starlink داخل ایران (No Ingress)

> ⚠️ این سناریو تنها راه عملی در Shutdown مطلق است.

---

## 🧠 توضیح سناریو B

- هیچ مسیر بین‌المللی از طریق ISP ایران وجود ندارد  
- VPS دیتاسنتری غیرقابل استفاده است  
- یک دستگاه فیزیکی داخل ایران مستقیماً به Starlink متصل می‌شود  

---

## 🗺 دیاگرام سناریو B

```
User
  |
Physical Server (Iran)
  |
Starlink
  |
Free Internet
```

---

## 🧩 پیش‌نیازهای سناریو B

- Starlink فعال داخل ایران  
- سیستم لینوکسی یا روتر مبتنی بر Linux  
- IP ایران (خانگی یا سازمانی)  

---

## 🔑 پیکربندی پایه شبکه

```bash
sysctl -w net.ipv4.ip_forward=1
iptables -t nat -A POSTROUTING -o starlink0 -j MASQUERADE
```

---

## 🔐 اتصال کاربران در سناریو B

دقیقاً مشابه سناریو A:

- OpenVPN
- WireGuard
- V2Ray

تنها تفاوت: خروجی NAT مستقیماً روی Interface استارلینک انجام می‌شود.

---

## ⚖️ Load Management

- محدودسازی Bandwidth
- Session limits
- Queue management

---

## ⚡ Failover

- UPS
- مانیتورینگ لینک
- لینک جایگزین در صورت امکان

---

## 📊 Monitoring

- Netdata
- vnStat
- Prometheus (اختیاری)

---

## 🧠 جمع‌بندی نهایی

- **Scenario A** زمانی جواب می‌دهد که Ingress برقرار باشد  
- **Scenario B** تنها راه در Shutdown مطلق است  
- این دو سناریو جایگزین هم نیستند؛ مکمل هم هستند  
